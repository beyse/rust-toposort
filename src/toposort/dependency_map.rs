// use std::collections::HashMap;

// #[allow(dead_code)]
// /// The dependency map is a map from a node to a vector of nodes.
// /// It can model either predecessors or successors.
// pub type HashMap<T, Vec<T>> = HashMap<T, Vec<T>>;
